let escolhaCores = document.getElementsByClassName('color');
let cores = ['black', 'red', 'blue', 'green'];
for (i = 0; i < escolhaCores.length; i++) {
  console.log(escolhaCores[i].style.backgroundColor = cores[i]);
}

